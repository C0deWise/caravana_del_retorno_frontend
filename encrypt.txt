# Encriptar (Retirar espacios vacios, comentarios y demas elementos que no son variables)
sops -e --input-type dotenv --output-type dotenv .env.local > env.encrypted

# Desencriptar
sops -d --input-type dotenv --output-type dotenv env.encrypted > .env.local
